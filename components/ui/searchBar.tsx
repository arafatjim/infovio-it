import { Search } from 'lucide-react'
import React from 'react'

const SearchBar = () => {
  return (
    <div>
      <Search className='text-white w-5 h-5 hover:text-logo-primary cursor-pointer' />
    </div>
  )
}

export default SearchBar
